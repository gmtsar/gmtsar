#! /usr/bin/env python3
"""batch_processing — top-level driver: preproc + align + intf for a stack.

PLAN Phase 2 SCAFFOLD — not yet implemented. Reference: gmtsar/csh/batch_processing.csh
(365 lines). End-to-end driver: reads data.in + intf.in + config, runs
pre_proc_batch → align_batch → intf_batch (+ optional unwrap_parallel).

Usage:  batch_processing SAT batch.config

This is the highest-level batch entry point. After porting, this would
replace the typical csh recipe pattern:
  pre_proc_batch.csh SAT data.in
  align_batch.csh RAW 1 data.in
  intf_batch.csh intf.in batch.config
"""
import sys


def batch_processing():
    if len(sys.argv) != 3:
        sys.exit(
            "Usage: batch_processing SAT batch.config\n"
            "  PLAN Phase 2 scaffold — not yet implemented."
        )
    raise NotImplementedError("batch_processing: Phase 2 scaffold")


if __name__ == "__main__":
    batch_processing()
