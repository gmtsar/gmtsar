#! /usr/bin/env python3
"""intf_batch — form a batch of interferograms from a pairs list.

PLAN Phase 2 SCAFFOLD — not yet implemented. Reference: gmtsar/csh/intf_batch.csh
(282 lines). Reads intf.in (each line "ref:rep"), creates intf/<ref>_<rep>/
working dirs, runs intf + filter (+ snaphu + geocode optionally) per pair.

Usage:  intf_batch intf.in batch_tops.config

batch_tops.config is the standard GMTSAR batch config (filter_wavelength,
dec_factor, range_dec, azimuth_dec, etc.).
"""
import sys


def intf_batch():
    if len(sys.argv) != 3:
        sys.exit(
            "Usage: intf_batch intf.in batch_tops.config\n"
            "  PLAN Phase 2 scaffold — not yet implemented."
        )
    raise NotImplementedError("intf_batch: Phase 2 scaffold")


if __name__ == "__main__":
    intf_batch()
