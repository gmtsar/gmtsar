#! /usr/bin/env python3
"""proj_ll2ra — Python port of csh proj_ll2ra.csh (PLAN Phase 1).

Project a GRD file from lon/lat coordinates into range/azimuth coordinates.
The inverse of proj_ra2ll (already ported, see gmtsar/python/utils/proj_ra2ll).

Usage:  proj_ll2ra trans.dat phase_ll.grd phase_ra.grd [filter_wavelength]

Inputs:
  trans.dat        — file generated by llt_grid2rat (r a topo lon lat)
  phase_ll.grd     — GRD file in lon/lat coordinates
  filter_wavelength — optional, samples every 1/4 wavelength (meters)
Output:
  phase_ra.grd     — same data in radar (range/azimuth) coordinates

This is a SCAFFOLD — not implemented.
Reference: gmtsar/csh/proj_ll2ra.csh (71 lines).

Implementation notes:
- Mirrors the existing proj_ra2ll Python (gmtsar/python/utils/proj_ra2ll)
  but with input/output coordinate systems swapped.
- The two scripts share ~80% of their logic (grd2xyz, surface, grdtrack,
  blockmedian, xyz2grd). Strongly consider extracting a shared helper
  module (e.g. `proj_lib.py`) used by both.
- Sample-spacing logic for the filter_wavelength arg is identical to
  proj_ra2ll — copy that branch.
"""
import sys


def proj_ll2ra():
    if len(sys.argv) not in (4, 5):
        sys.exit(
            "Usage: proj_ll2ra trans.dat phase_ll.grd phase_ra.grd [filter_wavelength]\n"
            "  trans.dat        - file generated by llt_grid2rat (r a topo lon lat)\n"
            "  phase_ll.grd     - input GRD in lon/lat coordinates\n"
            "  phase_ra.grd     - output GRD in radar coordinates\n"
            "  filter_wavelength - optional; samples every 1/4 wavelength (meters)"
        )
    raise NotImplementedError("proj_ll2ra: PLAN Phase 1 scaffold — not yet implemented")


if __name__ == "__main__":
    proj_ll2ra()
