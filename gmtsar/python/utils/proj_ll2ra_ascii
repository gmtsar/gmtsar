#! /usr/bin/env python3
"""proj_ll2ra_ascii — Python port of csh proj_ll2ra_ascii.csh (PLAN Phase 1).

ASCII (text-file) variant of proj_ll2ra. Reads lon/lat/value triples from
a text file and projects to range/azimuth/value triples using trans.dat.

Usage:  proj_ll2ra_ascii trans.dat input_ll.txt output_ra.txt

This is a SCAFFOLD — not implemented.
Reference: gmtsar/csh/proj_ll2ra_ascii.csh (62 lines).

Implementation notes:
- Same kernel as proj_ll2ra but with text I/O instead of GRD I/O.
- Use numpy.loadtxt / savetxt for the ASCII handling.
- Should share its coordinate-transform kernel with proj_ll2ra via a
  proj_lib.py helper.
"""
import sys


def proj_ll2ra_ascii():
    if len(sys.argv) != 4:
        sys.exit(
            "Usage: proj_ll2ra_ascii trans.dat input_ll.txt output_ra.txt"
        )
    raise NotImplementedError("proj_ll2ra_ascii: PLAN Phase 1 scaffold — not yet implemented")


if __name__ == "__main__":
    proj_ll2ra_ascii()
