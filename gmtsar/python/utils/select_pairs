#! /usr/bin/env python3
"""select_pairs — Python port of csh select_pairs.csh (PLAN Phase 1).

Given a baseline_table.dat, select interferogram pairs within the supplied
temporal and perpendicular-baseline thresholds, and write the list to
intf.in. Also produces a network plot (baseline.ps → .pdf).

Usage:  select_pairs baseline_table.dat threshold_time threshold_baseline
Output: intf.in (one "<ref>:<rep>" pair per line)
        baseline.pdf (network plot)

This is a SCAFFOLD — not implemented.
Reference: gmtsar/csh/select_pairs.csh (56 lines).

Implementation notes:
- baseline_table.dat columns (from baseline_table): name orb_id day_of_year ... bperp ...
- Algorithm is O(N^2) pair enumeration with threshold filter:
    for line1 in table:
        for line2 in table:
            if t1 < t2 and (t2 - t1) < dt and abs(b1 - b2) < db:
                append "name1:name2" to intf.in
- Python version should use numpy + vectorized comparison (still O(N^2)
  memory but much faster) or itertools.combinations for cleaner code.
- The legacy csh uses an inner `gmt psxy` per pair to draw network edges,
  which is the slow part. Python should batch all edges and call psxy once.
- Year-axis offset (legacy: `+2014`) should come from the data, not be
  hardcoded — that's a latent bug worth fixing.
"""
import sys


def select_pairs():
    if len(sys.argv) != 4:
        sys.exit(
            "Usage: select_pairs baseline_table.dat threshold_time threshold_baseline\n"
            "  outputs: intf.in"
        )
    raise NotImplementedError("select_pairs: PLAN Phase 1 scaffold — not yet implemented")


if __name__ == "__main__":
    select_pairs()
