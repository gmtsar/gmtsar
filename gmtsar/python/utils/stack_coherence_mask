#! /usr/bin/env python3
"""PLAN Phase 4 (SBAS / time series) scaffold — pending Python port.

Reference: see gmtsar/csh/<same name>.csh.
Phase 4 is blocked on a multi-pair test fixture (see PLAN.md §8 open
questions). The new sbas tier in tests/cases.py (added v1.10.0) lists
the available topex datasets:
  ALOS_Indio_SBAS, S1A_Stack_CPGF_T173, ALOS_Hawaii_stack,
  ENVI_2907_stack, kilauea_timeseries_sentinel_data.
"""
import os, sys
sys.exit(
    f"{os.path.basename(__file__)}: PLAN Phase 4 scaffold — not yet implemented.\n"
    f"See gmtsar/csh/{os.path.basename(__file__)}.csh for the legacy CLI."
)
